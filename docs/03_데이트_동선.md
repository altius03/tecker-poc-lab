# 데이트 동선 탐색 프로그램

## 0. 피드백용 요약

| 항목 | 내용 |
| --- | --- |
| 프로덕트 제목 | 데이트 동선 탐색 프로그램 |
| 한 줄 소개 | 자연어로 데이트 조건을 입력하면, 지도 위에서 장소를 검색·필터링하고 여러 곳을 잇는 데이트 코스까지 설계해 길 찾기로 연결해 주는 서비스 |
| 핵심 문제 | 기존 지도 앱과 블로그/SNS 추천은 복합 조건, 예산, 이동수단, 시간대, 동선을 한 번에 반영하지 못해 사용자가 여러 앱을 오가며 수동으로 코스를 조립해야 함 |
| 핵심 사용자 | 데이트 장소·동선 고민이 잦은 커플 |
| 중심 기능 | 자연어 조건 해석, 지도 기반 장소 검색, 거리 계산 및 필터링, 동선 설계, 길찾기 연결, 리뷰 요약 |
| 데모 포인트 | 자연어 입력 → 구조화 조건 추출 → 장소 후보 검색 → 동선 생성 → 지도 렌더링 및 길찾기 연결 |
| 주요 리스크 | 단순 추천 서비스로 보일 위험, 리뷰 데이터 확보, API 쿼터/과금, 무드 키워드 모호성 |
| 이후 보완 | 수익화 방향, 유사 서비스, 멘토 피드백, 실제 API 사용 범위 확인 |

---

## 1. 서비스 개요

### 1-1. 프로덕트 제목

데이트 동선 탐색 프로그램

### 1-2. 한 줄 소개

자연어로 데이트 조건을 입력하면, 지도 위에서 장소를 검색·필터링하고 **여러 곳을 잇는 데이트 코스**까지 설계해 길 찾기로 연결해 주는 서비스. (하나의 장소가 아닌 코스가 메인 테마)

### 1-3. 프로젝트 목표 / 핵심 컨셉

이 프로젝트는 **추천 알고리즘**이 아니라 **"자연어 조건 해석 → 공간 필터링 → 동선 설계"**라는 **명시적·설명 가능한 파이프라인**이다.

---

## 2. 문제 정의

- 커플의 반복적인 고민 1순위는 **"오늘 어디서 뭐 하지?"** 이고, 실제로는 **여러 장소를 순서대로 엮는 동선 짜기**가 더 어렵다.
- 기존 지도 앱의 단순 필터(카테고리·별점순 정렬)는 **복합 조건**("○○역 근처, 예산 10만 원, 분위기 좋은 카페랑 파스타집, 걸어서 이동")을 반영하지 못한다.
- 블로그·SNS의 "데이트 코스 추천" 글은 **내 위치·예산·시간에 맞춤이 아니고**, 지도/길찾기로 바로 이어지지도 않는다.

**결과:** 앱 여러 개를 옮겨 다니며(검색앱 → 지도앱 → 길찾기앱) 수동으로 동선을 조립하는 비효율이 발생한다.

---

## 3. 타깃 사용자

**타깃:** 데이트 장소·동선 고민이 잦은 커플

**시나리오 예시 (자연어 입력)**

`"성수역 근처에서 저녁 데이트할 건데, 예산 12만 원 안쪽, 분위기 좋은 파스타집이랑 디저트 카페, 걸어서 이동 가능한 코스로 짜줘"`

↓ 출력: 지도 위 코스(파스타집 → 도보 8분 → 디저트 카페) + 각 장소 리뷰 요약 + provider 정책에 맞는 길찾기 버튼

---

## 4. 주요 기능

### 4-1. 자연어 조건 해석

- LLM으로 입력을 구조화 JSON으로 추출(지역·반경·예산·무드·카테고리 세트·이동수단·시간대).

### 4-2. 지도 기반 장소 검색

- 지역·카테고리별 장소 후보 조회.

### 4-3. 거리 계산 및 필터링

- 반경/평점/리뷰 수 조건으로 후보 압축

### 4-4. 동선 설계

- 선택된 장소들을 잇는 **경유지 경로**를 생성하고 이동수단·소요시간을 계산.

### 4-5. 길찾기 연결

- 완성된 코스를 지도 앱 길찾기로 연결한다. 단, 리뷰/장소 데이터와 지도 제공자를 섞지 않도록 provider mode를 둔다.
- Google Places 리뷰를 쓰는 코스는 Google Maps 표시/길찾기로 연결하고, Kakao 길찾기를 쓰는 코스는 Kakao Local 또는 자체/제휴 리뷰 데이터만 사용한다.

### 4-6. 리뷰 요약

- 각 장소의 리뷰를 짧게 요약하고 **코스 전체 분위기를 한 줄로 정리.**

---

## 5. 서비스 흐름

### 5-1. 1~2분 발표 흐름

1. 사용자가 지역, 예산, 무드, 카테고리, 이동수단을 자연어로 입력한다.
2. LLM이 입력 문장을 구조화 조건으로 변환한다.
3. 장소 검색 결과를 거리, 평점, 리뷰 수, 무드 조건으로 압축한다.
4. 코스 빌더가 2~4개 장소를 방문 순서로 묶는다.
5. 지도에 코스를 표시하고 경유지 길찾기 링크로 연결한다.
---

## 6. 챌린징 포인트 / PoC

### 6-1. 검증할 챌린징 포인트

| 챌린징 포인트 | 확인할 내용 | 추가 확인 필요 |
| --- | --- | --- |
| 자연어 조건 구조화 | 사용자의 데이트 조건을 지역·예산·무드·카테고리·이동수단·시간대 JSON으로 변환할 수 있는지 확인 | 테스트할 입력 예시 확인 필요 |
| 장소 후보 검색 | 구조화된 조건으로 장소 후보를 가져올 수 있는지 확인 | 실제 API 사용 범위 확인 필요 |
| 동선 생성 | 2~4개 장소를 방문 순서로 묶고 이동 거리/시간을 계산할 수 있는지 확인 | 경로 계산 방식 확인 필요 |
| 근거 제시 | 각 장소가 코스에 포함된 이유를 거리·평점·리뷰 키워드 등으로 설명할 수 있는지 확인 | 리뷰 데이터 출처 확인 필요 |
| 길찾기 연결 | 완성된 코스를 지도 앱 경유지 길찾기로 연결할 수 있는지 확인 | 사용할 지도 앱 확인 필요 |
---

## 7. 차별점 / 어필 포인트

| 구분 | 일반 지도 앱 필터 | 본 프로젝트 |
| --- | --- | --- |
| 입력 | 카테고리 + 정렬 옵션 | **자연어 복합 조건**(지역·예산·무드·시간·이동수단) |
| 출력 | 장소 목록 | **한 곳 + 여러 곳을 잇는 코스(동선)** |
| 근거 | 별점순(불투명) | **각 장소가 뽑힌 조건 근거를 명시**(거리·평점·키워드 매칭) |
| 연결 | 개별 길찾기 | **코스 전체를 경유지 길찾기로 한 번에 연결** |

### 7-1. 차별화 포인트 3가지

1. **동선 설계:** 단일 장소가 아니라 **다중 경유 경로**를 만든다. → 기술적 난도의 핵심이자 다른 팀과의 차별점.
2. **맥락 기반 자연어:** "분위기 있는", "조용한", "야경 보이는" 같은 **무드 키워드**를 구조화 조건으로 변환.
3. **근거 투명성:** "왜 이 장소가 코스에 들어갔는지"(3km 이내 · 평점 4.6 · '분위기' 리뷰 다수)를 사용자에게 그대로 보여준다.

---

## 8. 기술 챌린지 / 기술적 가능성

### 8-1. 위치 기반 검색 / 공간 인덱싱

사용자의 위치나 선택한 지역을 기준으로 주변 후보 장소를 빠르게 찾습니다. 먼저 대략적인 범위를 좁힌 뒤, 실제 거리 계산으로 정확도를 높입니다.

### 8-2. 다중 지점 동선 설계

여러 장소를 방문할 때 어떤 순서로 이동하면 효율적인지 계산합니다. 이동수단별 예상 소요시간을 반영해 현실적인 코스를 구성합니다.

### 8-3. 자연어 조건 구조화

“조용한 분위기”, “데이트하기 좋은 곳”, “비 오면 실내 위주” 같은 표현을 검색 가능한 조건으로 변환합니다. 이를 위해 프롬프트와 응답 스키마를 안정적으로 설계합니다.

### 8-4. 지도 API 연동 및 호출 최적화

지도 API를 활용해 장소, 거리, 이동시간 정보를 가져옵니다. API 사용량을 줄이기 위해 결과를 캐싱하고, 필요한 요청만 묶어서 처리합니다.

---

## 9. 리스크 및 대응

| 리스크 | 내용 | 대응 |
| --- | --- | --- |
| **리뷰 데이터 확보** | 카카오/네이버 API는 평점·리뷰 텍스트를 공식 제공하지 않음 | 리뷰 요약 기능은 유지한다. MVP는 Google Places `reviews`/`reviewSummary`와 Google Maps 화면으로 구성하고, Kakao 모드는 자체/제휴 리뷰 데이터가 있을 때만 리뷰 요약을 켠다. |
| **API 쿼터/과금** | 구글 Places는 필드에 따라 과금 등급 상승, 카카오는 일/월 쿼터 | FieldMask 최소화, 결과 캐싱, 데모 지역 한정, 리뷰 필드 호출 횟수 제한으로 기능을 유지 |
| **경유지 최적화 복잡도** | 지점 수 늘면 순서 계산 부담 | 동선 기능은 유지하되 MVP는 3~4개 장소에 대해 휴리스틱/거리 기반 정렬로 구현하고, 고급 최적화는 확장 과제로 둔다. |
| **무드 키워드 모호성** | "분위기 좋은"의 기준이 주관적 | 리뷰 텍스트 임베딩/키워드 매칭으로 근거 제시, 완벽 매칭보다 '근거 투명성'에 집중 |

---

## 10. 상업성 / 수익화

- 확인 필요: 수익화 방향 선택 필요
---

## 11. 확장 가능성

- **일정 기반 동선 추천** — "오후 2시~밤 10시" 시간 배분에 맞춘 코스.
- **혼잡도 예측** — 시간대별 대기·혼잡도 반영.
- **RAG 기반 장소 Q&A** — "웨이팅 길어요?", "주차 돼요?" → 리뷰 검색·요약 답변.
- **리뷰 근거 기반 매칭** — 별점이 아닌 리뷰 내용 임베딩으로 무드 의도 매칭.
- **멀티모달 검색** — 사진(분위기/인테리어) → 비슷한 장소 코스.
- **음성 에이전트** — 이동 중 음성으로 "근처 산책 코스 짜줘".

---

## 12. 유사 서비스 / 참고 자료

- 확인 필요: 참고할 유사 서비스 이름이나 링크가 필요함
---

## 13. 멘토 피드백 / 메모

- 확인 필요: 공유된 멘토 피드백이 있으면 추가 필요
---

## 14. 다음 보완 항목

- 수익화 방향 확정
- 실제 API 사용 범위 확인
- 테스트할 자연어 입력 예시 확인
- 참고할 유사 서비스 확인
- 멘토 피드백 확인

---

## API 호출 과금 검토

| API | 과금/쿼터 판단 | MVP 대응 |
| --- | --- | --- |
| Kakao Local API | 일간 무료 제공 쿼터 100,000건. 추가 쿼터 사용 시 키워드/카테고리 장소 검색 2원/건 | 유료 API 사용 안 함, 데모 지역/검색 횟수 제한, 캐싱 적용 |
| Kakao 지도 SDK | 일간 무료 제공 쿼터 300,000건. 추가 쿼터 사용 시 0.1원/건 | PoC는 무료 쿼터 내 충분 |
| Google Places API | Place Details의 `reviews`, `reviewSummary`는 Enterprise + Atmosphere SKU로 비용 리스크 큼 | Google mode에서 최종 후보 2~3개에만 호출 |
| Google Routes API | pay-as-you-go. Compute Routes는 요청당, Matrix는 element 단위 과금 | 서버 경로 계산보다 지도 URL 길찾기 우선 |
| Google Maps URLs | API key 없이 사용 가능 | 경유지 길찾기 데모는 URL 링크로 처리 |

