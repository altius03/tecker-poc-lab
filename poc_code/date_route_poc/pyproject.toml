[project]
name = "date-route-poc"
version = "0.1.0"
description = "Python CLI PoC for date-route condition capability checks with Google Places and OpenAI."
requires-python = ">=3.11"
dependencies = [
  "requests",
  "python-dotenv",
  "pydantic",
  "typer",
  "rich"
]

[tool.uv]
package = false
