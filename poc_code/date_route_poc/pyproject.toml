[project]
name = "date-route-poc"
version = "0.1.0"
description = "Python CLI PoC for natural-language date route planning with Kakao Local candidates."
requires-python = ">=3.11"
dependencies = [
  "requests",
  "python-dotenv",
  "pydantic",
  "typer",
  "rich"
]

[tool.uv]
package = false
